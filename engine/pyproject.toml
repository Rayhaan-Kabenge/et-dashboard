[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "et_engine"
version = "1.0.0"
description = "Validated ET irrigation-scheduler engine (vendored, do not modify)"
requires-python = ">=3.10"
dependencies = [
    "pyfao56>=1.3.0",
]

[tool.setuptools]
packages = ["et_engine"]
